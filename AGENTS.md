# 本项目约定

## 提交说明

**全部用中文。** 不写英文 type 前缀（不用 `feat:` / `fix:` / `chore:`）。

格式：

```
<中文动词开头的一句话，说明改了什么>

正文：为什么这么改、验证了什么。
涉及多个模块时用【】分节。
```

例如：

```
新增：pi-memory 的跨进程写锁

【问题】两个 pi 同时 flush 时是 last-write-wins，后写的会静默吃掉先写的记忆。

【做法】写前取文件独占锁（O_EXCL 创建，原子），拿到后重新读取再应用增量。
瞬态失败（锁冲突）放回队列重试；永久失败（目录已删）丢弃并说明。

验证：新增 8 项断言，含「锁冲突时条目不丢」与「拿锁后重读不覆盖别人的并发写入」。
```

## 代码注释

**用中文。** 注释解释"为什么"，不复述"做了什么"（代码本身已经说明了）。

非显然的取舍要把理由写下来，尤其是踩过的坑——这些是代码本身读不出来的信息。

## 依赖 pi 的内部行为时

**先在源码里核实，不要凭猜测。** 本项目多处依赖 pi 的非公开行为，例如：

- `AGENTS.md` 按 cwd **逐级向上查找**并全量注入
- `onTerminalInput` 支持"不 consume 就放行"，而 `registerShortcut` 一旦匹配就完全吞掉按键
- pi 内置选择器（`/resume`、`/tree`）不走扩展 UI 通路，扩展无法检测其是否打开
- compaction 摘要的结构化小节名

核实过的结论要写进注释，并注明依据（哪个文件、哪个函数），方便日后 pi 升级时回查。

## 测试

改动必须让 `npm test` 保持全绿（tasks 24 项 + memory 86 项），并且 `npm run check` 零错误。

测试**不得触碰**真实 `~/.pi` 与真实家目录——用 `test/_harness.mjs` 的沙箱。
